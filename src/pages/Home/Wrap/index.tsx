import * as React from 'react';
import WrapCantainer from '@/components/Container';
interface IAppProps {}

const Wrap: React.FunctionComponent<IAppProps> = (props) => {
  return <WrapCantainer />;
};

export default Wrap;
